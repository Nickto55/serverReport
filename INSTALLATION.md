# Installation Guide

Полное руководство по установке и настройке проекта ServerReport.

## 🚀 Быстрая установка (рекомендуется)

### На Linux/macOS

```bash
# 1. Сделайте скрипт исполняемым
chmod +x setup.sh

# 2. Запустите установку
./setup.sh
```

### На Windows

```batch
# Запустите батник в CMD или PowerShell
setup.bat
```

## 📋 Что делает установочный скрипт

1. ✅ **Проверка зависимостей** - Docker, Docker Compose, Git
2. ✅ **Проверка портов** - Убеждается, что необходимые порты свободны
3. ✅ **Создание конфигурации** - Интерактивное создание `.env` файла
4. ✅ **Ввод токенов** - Запрос Discord и Telegram токенов
5. ✅ **Сборка образов** - Сборка Docker образов для всех сервисов
6. ✅ **Запуск сервисов** - Запуск контейнеров с помощью Docker Compose
7. ✅ **Проверка здоровья** - Проверка статуса всех сервисов
8. ✅ **Справка по использованию** - Инструкции для начала работы

## 📦 Требования

### Обязательные

- **Docker** 20.10+ - [Установить Docker](https://docs.docker.com/get-docker/)
- **Docker Compose** 2.0+ - [Установить Docker Compose](https://docs.docker.com/compose/install/)
- **Git** - [Установить Git](https://git-scm.com/)

### Для локальной разработки (опционально)

- **Node.js** 18+ - [Установить Node.js](https://nodejs.org/)
- **PostgreSQL** 15+ - [Установить PostgreSQL](https://www.postgresql.org/download/)

## 🔑 Получение токенов ботов

### Discord Bot Token

1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Нажмите "New Application"
3. Дайте боту имя (например, "ServerReport Bot")
4. Перейдите на вкладку "Bot"
5. Нажмите "Add Bot"
6. Под именем бота нажмите "Copy" для копирования токена
7. Используйте этот токен в скрипте установки

**Необходимые разрешения бота:**
- Read Messages/View Channels
- Send Messages
- Read Message History
- Manage Messages

### Telegram Bot Token

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Дайте боту имя
4. Дайте боту юзернейм (должен заканчиваться на "bot")
5. Скопируйте полученный токен
6. Используйте этот токен в скрипте установки

## 🔐 Переменные окружения

Скрипт попросит вас ввести следующие значения:

### Database
- `DB_USER` - Пользователь PostgreSQL (по умолчанию: `serverreport`)
- `DB_PASSWORD` - Пароль БД (по умолчанию: `serverreport_pass`)
- `DB_NAME` - Имя БД (по умолчанию: `serverreport`)
- `DB_PORT` - Порт PostgreSQL (по умолчанию: `5432`)

### Website
- `WEBSITE_PORT` - Порт веб-приложения (по умолчанию: `3000`)
- `NODE_ENV` - Окружение (development/production)

### Bots
- `DISCORD_TOKEN` - Токен Discord бота (обязательно)
- `TELEGRAM_TOKEN` - Токен Telegram бота (обязательно)

### Security
- `JWT_SECRET` - Секретный ключ для JWT (автогенерируется)
- `ADMIN_USER` - Имя администратора (по умолчанию: `admin`)
- `ADMIN_PASSWORD` - Пароль администратора

## 🐳 Docker Compose компоненты

Скрипт запустит следующие сервисы:

| Сервис | Порт | Описание |
|--------|------|---------|
| Website | 3000 | Express.js REST API |
| PostgreSQL | 5432 | База данных |
| Discord Bot | N/A | Discord интеграция |
| Telegram Bot | N/A | Telegram интеграция |

## 📝 Структура файла .env

После установки файл `.env` будет выглядеть так:

```env
# Database Configuration
DB_USER=serverreport
DB_PASSWORD=serverreport_pass
DB_NAME=serverreport
DB_PORT=5432

# Website Configuration
WEBSITE_PORT=3000
NODE_ENV=development

# Discord Bot Configuration
DISCORD_TOKEN=your_discord_token

# Telegram Bot Configuration
TELEGRAM_TOKEN=your_telegram_token

# Admin Configuration
ADMIN_USER=admin
ADMIN_PASSWORD=admin_password

# JWT Secret
JWT_SECRET=your_jwt_secret_key
```

## ✅ Проверка установки

После успешной установки проверьте статус:

```bash
cd docker
docker-compose ps
```

Вы должны увидеть все сервисы со статусом "Up".

### Проверить логи

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f website
docker-compose logs -f postgres
docker-compose logs -f discord-bot
docker-compose logs -f telegram-bot
```

### Проверить веб-приложение

Откройте в браузере:
```
http://localhost:3000
```

Должна отобразиться главная страница.

### Проверить базу данных

```bash
# Подключиться к БД
docker-compose exec postgres psql -U serverreport -d serverreport

# Просмотреть таблицы
\dt

# Выход
\q
```

## 🛠️ Решение проблем

### Docker Compose команды не работают

**Решение:** Убедитесь, что вы находитесь в папке `docker/`:

```bash
cd docker
docker-compose up -d
```

### Порт уже используется

**Решение:** Измените порт в `.env` или остановите приложение, использующее порт:

```bash
# Linux/macOS - найти процесс на порту 3000
lsof -i :3000
kill -9 <PID>

# Windows - в PowerShell
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### Бот не отвечает

1. Проверьте, что токен правильно введен в `.env`
2. Убедитесь, что сервис запущен: `docker-compose logs discord-bot`
3. Проверьте, что бот добавлен на сервер/чат
4. Убедитесь, что бот имеет необходимые разрешения

### БД не инициализируется

**Решение:** Удалите существующие контейнеры и переустановите:

```bash
cd docker
docker-compose down -v
docker-compose up -d
```

Флаг `-v` удалит все данные, используйте с осторожностью!

## 🔄 Переустановка

Если нужно переустановить проект:

```bash
# 1. Остановить сервисы
cd docker
docker-compose down -v

# 2. Удалить .env (опционально)
rm ../config/.env

# 3. Запустить скрипт заново
cd ..
./setup.sh  # или setup.bat на Windows
```

## 📚 Дальнейшие шаги

1. **Создайте первого пользователя** через веб-интерфейс
2. **Получите админа** - скопируйте ID пользователя и обновите роль:

```bash
docker-compose exec postgres psql -U serverreport -d serverreport
UPDATE users SET role='admin' WHERE id=1;
```

3. **Настройте боты** - добавьте их на серверы/чаты
4. **Изучите документацию:**
   - [README.md](README.md) - Обзор проекта
   - [DEVELOPMENT.md](DEVELOPMENT.md) - Разработка
   - [API_REFERENCE.md](API_REFERENCE.md) - API документация

## 📞 Помощь

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Прочитайте [DEVELOPMENT.md](DEVELOPMENT.md)
3. Проверьте [API_REFERENCE.md](API_REFERENCE.md)
4. Убедитесь, что все зависимости установлены

---

**Готово!** 🎉 Ваш ServerReport полностью настроен и готов к использованию.
